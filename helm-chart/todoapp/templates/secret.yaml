apiVersion: v1
kind: Secret
metadata:
  name: {{ include "todoapp.fullname" . }}-{{ .Values.secret.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.secret.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
